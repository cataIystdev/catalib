"""Общая конфигурация pytest.

Каталог ``src`` добавлен в путь импорта через ``pythonpath`` в
``pyproject.toml``, поэтому пакет ``catalib`` импортируется без установки.
Файл намеренно не содержит логики — он закрепляет наличие пакета фикстур.
"""
